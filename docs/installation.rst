============
Installation
============

At the command line::

    $ easy_install bitonic_tour

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bitonic_tour
    $ pip install bitonic_tour
